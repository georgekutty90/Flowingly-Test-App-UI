# Flowingly-Test-App-UI
This project is a TypeScript + React application scaffolded with Vite. It provides an XML / Email parsing UI that submits content to a backend parser and displays parsed JSON output or validation errors.
